name: Final APK Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install Dependencies
        run: npm install

      - name: Build Web Project
        run: npm run build

      - name: Capacitor Sync
        run: npx cap sync android

      - name: Fix Android SDK and Gradle Wrapper
        run: |
          cd android
          # 1. Forcer la version du SDK à 36 dans variables.gradle
          sed -i 's/compileSdkVersion = 34/compileSdkVersion = 36/g' variables.gradle
          sed -i 's/targetSdkVersion = 34/targetSdkVersion = 36/g' variables.gradle
          
          # 2. Réparer le Gradle Wrapper corrompu
          rm -f gradle/wrapper/gradle-wrapper.jar
          gradle wrapper --gradle-version 8.7
          chmod +x gradlew

      - name: Build APK
        run: |
          cd android
          ./gradlew assembleDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Masrof-Final-APK
          path: android/app/build/outputs/apk/debug/*.apk
